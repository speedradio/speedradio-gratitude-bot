{
  "name": "gratitude-bot",
  "version": "1.0.0",
  "description": "Slack gratitude & karma bot — serverless on Vercel",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@vercel/kv": "^2.0.0"
  },
  "devDependencies": {
    "vercel": "^34.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
